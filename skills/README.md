This directory contains packaged agent skills that ship with the CLI.

The `privacy-pools/` subdirectory contains the public Privacy Pools skill for
agent hosts. Its `SKILL.md` is the entrypoint and companion reference material
lives under `privacy-pools/references/`.

If more bundled skills are added later, keep them as sibling directories under
`skills/`.
