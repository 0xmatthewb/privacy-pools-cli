let parsedVerboseLevel = 0;

export function setParsedVerboseLevel(level: number): void {
  parsedVerboseLevel = level;
}

export function getParsedVerboseLevel(): number {
  return parsedVerboseLevel;
}
